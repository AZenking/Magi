export enum TaskStatus {
  PENDING = "pending",
  RUNNING = "running",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum EpgSourceType {
  XMLTV = "xmltv",
  API = "api",
  SCRAPER = "scraper",
}

export enum ChannelStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
