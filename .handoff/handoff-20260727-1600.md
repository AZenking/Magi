# Handoff — US1 MVP implementation complete; T020/T046/T047 deferred

## Current objective

Continue `004-safe-operations-workflow`. **45/136 tasks done.** Phase 1 + Phase 2 Foundation (minus T020) + US1 tests + US1 implementation (T035–T048) complete. Next: Phase 4 US2 (channel lifecycle) or resolve deferred items.

## Deferred items (3)

1. **T020** — pre-existing Drizzle snapshot drift blocks `db:generate`. Resolve via `drizzle-kit introspect` in a TTY terminal against live `magi-postgres`, then generate the additive migration. Unblocks T028 integration tests.
2. **T046** — source-list-page M3U sync → preview integration. The file is in the dirty UI migration worktree; integrate during UI migration merge to avoid conflicts. `OperationPreview` (T045) + `usePreparePreview` (T044) are ready to wire.
3. **T047** — epg-matching route → preview integration. Same reason as T046.

## Work completed this session (T037–T048)

### Worker use cases (T037–T040)
- **T037**: `prepare-m3u-sync.use-case.ts` (download/parse/stage snapshot/diff/summary) + `apply-m3u-sync.use-case.ts` (stable upsert preserving id/operator/health + mark-missing, no delete).
- **T038**: `reconcile-canonical-channels.use-case.ts` (incremental membership linking; preserves canonical IDs; deactivates missing members).
- **T039**: `prepare-epg-match.use-case.ts` (XMLTV readiness check + 4-class classification + preserved-lock counting) + `apply-epg-match.use-case.ts` (approved-bindings-only apply; skips manual-locked).
- **T040**: `create-recovery-point.use-case.ts` (per-object snapshot, failure⇒zero-write) + `prepare-recovery-restore.use-case.ts` (restore preview) + Worker `apply-recovery-restore.use-case.ts` (ordered restore).
- New domain ports: `ISourceSyncRepository`, `ICanonicalReconcileRepository`, `IEpgSyncRepository`, `IRestorePort`.

### Worker infra + registration (T041)
- `cleanup-operation-state.use-case.ts` (reference-safe expiry; 24h retention; lease reclaim after task-inactive confirm).
- `infrastructure/database/operation-execution.repository.ts` (implements IOperationExecutionRepository + ICleanupPort against Drizzle).
- `infrastructure/queue/operation-worker.ts` (registers operation-prepare/apply/restore/cleanup handlers).
- `infrastructure/queue/operation-cleanup-worker.ts` (hourly cleanup loop).
- `worker-bootstrap.ts` updated to register operation handlers + start cleanup worker on boot.
- Worker `schema.ts` extended to re-export all 15 new Safe Ops tables.

### API enqueue + HTTP (T042–T043)
- **T042**: `task-queue.adapter.ts` — deduplicationId takes precedence as BullMQ jobId; trace fields (requestId/changeSetId/fingerprint/scope/parent/idempotencyKey) injected into payload.
- **T043**: `operation.controller.ts` (POST /operations/previews, GET change-sets/{id}, GET items, PATCH items, POST apply with @Idempotent, POST cancel) + `operation.module.ts` + registered in `http.module.ts`. `application/operation-safety/index.ts` barrel created.

### Web (T044–T045)
- **T044**: `operation-queries.ts` — query keys, `useChangeSet`/`useChangeItems`/`usePreparePreview`/`useApplyChangeSet`/`useCancelChangeSet`, target-scoped pending. `apiClient` extended with `headers` support (for Idempotency-Key).
- **T045**: `operation-preview.tsx` (impact summary via Statistic, warnings/blockers via Alert, controlled confirmation Modal, single primary button) + `operation-impact-table.tsx` (paged TanStack Table, stable row keys). antd v6 visual language per T001.

### Rollout (T048)
- `rollout-runbook.md` (6-phase Parallel Change matrix, switch table, data gates G1–G5, stop conditions, rollback steps, current status).
- `safe-operations.config.ts` (API + Worker) — all defaults OFF.
- `docker/.env.example` — SAFE_OPS_* switches.

## Validation state (all green)

types/backend-core/api/worker/web: tsc **0**, lint **0**. Tests: backend-core 15, api 138 (+22 skip), worker 6 (+5 skip), web 7 (+3 skip). Worker architecture-boundary test (T032) still passes — application/domain never import Drizzle/BullMQ/fs.

## US1 checkpoint status

- Quickstart Scenario 1–5, 11: **code complete**; end-to-end run requires T020 migration (DB) + T046/T047 UI wiring.
- Foundation layer fully exercised by unit tests; integration tests (T028) skip until DB migration.
- Rollout runbook drafted — **must be reviewed before any shadow/enable**.

## Recommended next actions

1. **Resolve T020** in a TTY terminal (`drizzle-kit introspect` → regenerate snapshot → `db:generate` additive migration → run T019/T028).
2. **Wire T046/T047** during the UI migration merge (OperationPreview + usePreparePreview are ready).
3. **Begin Phase 4 US2** (channel lifecycle, T049–T061) — builds on the lifecycle fields + repository methods already in place.
4. Run `antd lint apps/web/src` (T133) on the new operation components before final UI review.

## Suggested skills

- `speckit-implement` — continue to US2.
- `drizzle` — T020 snapshot reconciliation.
- `ant-design` — T046/T047 + T133 lint.
