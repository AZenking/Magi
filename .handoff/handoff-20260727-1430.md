# Handoff — Phase 2 Foundation complete (T020 deferred); ready for US1 MVP

## Current objective

Continue `004-safe-operations-workflow`. **Phase 1 (T001–T005) + Phase 2 Foundation (T006–T019, T021–T027) are complete.** Only **T020** remains deferred (blocked by a pre-existing Drizzle snapshot/DB drift, documented below). Next major milestone: **Phase 3 US1 MVP (T028–T048)** — safe sync/match preview→apply→recovery.

**26/136 tasks done** (T001–T019, T021–T027). T020 is the sole unchecked Foundation task.

## Source of truth (read before continuing)

- `specs/004-safe-operations-workflow/{spec,plan,tasks,data-model,research,quickstart}.md`
- `specs/004-safe-operations-workflow/contracts/*`
- `specs/004-safe-operations-workflow/antd-research.md` (T001 — required before any UI task)
- `.specify/memory/constitution.md`
- Previous handoff: `.handoff/handoff-20260727-1330.md` (covers Phase 2a/2b/2c-detail)

## Work completed this session (T022–T027)

### Phase 2d — Domain models + ports (T022–T023) ✅
- **T022**: Extended channel/source/task domain models with optional Safe Ops fields (lifecycle, version, sourcePresence, manualEpg, scope/target/parent/result) + added new repository methods (upsertStable, updateIfVersion, findTrashed, findByScope, findSummary, reorder, etc.). Existing infrastructure repositories received stub implementations (throw `T0NN: not implemented yet`) so the interface satisfies and the 138 existing tests stay green. Real implementations land in their respective US tasks.
- **T023**: New domain modules — `domain/audit/` (AuditEvent model + IAuditRepository), `domain/backup/` (ConfigBackup model + IConfigBackupRepository), `domain/task-execution/scheduled-job.model.ts`. Shared `BackupObjectStorage` port defined in `packages/backend-core/src/backup/backup-object-storage.port.ts`.

### Phase 2e — Infrastructure repositories + HTTP (T024–T026) ✅
- **T024**: 5 new Drizzle repositories implementing T021 ports — `operation-change-set.repository.ts` (with reference-safe delete), `source-import-snapshot.repository.ts` (with collision-group counting), `recovery-point.repository.ts` (reference-safe expiry), `operation-lease.repository.ts` (atomic upsert-on-conflict, heartbeat, reclaim-if-expired), `idempotency.repository.ts` (tryRecord/saveResponse/deleteExpired with fingerprint match detection).
- **T025**: 4 more repositories — `audit-event.repository.ts` (append-only), `outbox.repository.ts` (enqueue/findPending/markPublished/markFailed), `scheduled-job.repository.ts` (updateIfVersion optimistic concurrency, recordRun projection), `config-backup.repository.ts` (metadata only, markExpired after object deletion).
- **T026**: NestJS HTTP infrastructure, all registered globally in `app.module.ts`:
  - `ProblemDetailsFilter` (`@Catch()`, RFC 9457 `application/problem+json`, stable `code` mapping, no stack leaks).
  - `RequestContextMiddleware` (generates/accepts `x-request-id`, AsyncLocalStorage for cross-boundary trace).
  - `precondition.ts` (`IfMatchRequiredGuard`, `parseIfMatch`, `etagFor`).
  - `IdempotencyInterceptor` (`@Idempotent(command)` decorator, 24h retention, 409 on key reuse with different fingerprint, replays cached response on match).

### Phase 2f — Worker Clean Architecture boundary (T027) ✅
- New Worker domain layer: `domain/job-execution/` (Job model, IJobExecutionRepository, IJobQueuePort) + `domain/operation-safety/` (IOperationExecutionRepository).
- `application/job-runner.ts` — framework-agnostic dispatcher; depends ONLY on domain ports + backend-core. Never imports Drizzle/BullMQ/fs (constitution III).
- `infrastructure/database/drizzle-job-execution.repository.ts` (Drizzle adapter for task milestones).
- `infrastructure/queue/bullmq-job-queue.adapter.ts` (BullMQ adapter with deduplicationId).
- `infrastructure/queue/worker-bootstrap.ts` — wires layers, validates payload shape, hands typed `Job` to JobRunner, structured lifecycle logs. `buildJobRunner()` exported for tests.
- Existing `main.ts` + `processors/*` preserved as legacy (still functional); they will be wrapped as handlers or replaced by Safe Operations use cases in US1.

## Validation state (all green, zero regressions)

- `@magi/types`: tsc 0 errors.
- `@magi/backend-core`: tsc 0 errors, **15 tests pass** (operation-diff), dist clean.
- `@magi/api`: tsc 0 errors, lint 0, **138 tests pass** (no regressions from schema refactor or interface extensions).
- `@magi/worker`: tsc 0 errors, lint 0, vitest configured (`passWithNoTests`).

## T020 — still deferred (pre-existing snapshot drift)

`apps/api/drizzle/meta/0000_snapshot.json` records an early schema (`epg_sources`, `tasks`, `channels.name/icon/url`) that drifted from the live DB long before this feature. `db:generate` therefore emits destructive DROPs. **This is NOT introduced by this work.** Journal restored to `0007`; no polluted migration files remain.

**Recommended resolution** (needs a TTY-capable terminal): `drizzle-kit introspect`/`pull` against the live `magi-postgres` DB to rebuild the snapshot baseline, then `db:generate` yields a pure additive migration (the 15 new tables + extended columns). T019 migration tests will then pass. **T022–T027 do NOT depend on T020** — they are pure code already compiled and tested.

## Important workspace state

- Dirty UI migration worktree (apps/web, packages/ui deletions) **preserved** — untouched.
- `apps/api/drizzle.config.ts` lists schema files explicitly (auth.ts + backend-core schema table files, excluding index.ts) to avoid `__exportStar` double-registration; uses `path.resolve(__dirname, "../../../../..")` for repo root.
- `drizzle-orm` aligned to 0.45.2 across api + backend-core + worker.
- API infrastructure repositories that received stub methods (channel/source/canonical/stream/sync-log) carry `throw new Error("T0NN: ...")` — these are filled in by their US tasks (T035, T056, T082-T083, T115, etc.).
- `pnpm-lock.yaml` updated.
- No commit created.

## Recommended next actions

1. **Resolve T020** in a TTY terminal (introspect/pull to rebuild snapshot baseline, then generate the additive migration + run T019 tests).
2. **Begin Phase 3 US1 MVP (T028–T048)** — the first deployable unit. Start with the red tests (T028–T034), then implementation (T035–T048). US1 establishes the safe sync/match preview→apply→recovery flow and reuses the entire Foundation layer built here.
3. Before editing code, inspect the dirty worktree and use CodeGraph for blast-radius on the processors being wrapped.

## Suggested skills

- `speckit-implement` — continue executing the US1 task plan.
- `drizzle` — for the T020 snapshot baseline reconciliation.
- `testing` — maintain red-green for US1 repository/application/worker tests.
- `nestjs-expert` — for the US1 HTTP controllers + module wiring (T043).
