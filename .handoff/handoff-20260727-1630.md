# Handoff — US1 + US2 backend complete; Web tasks + T020 deferred

## Current objective

Continue `004-safe-operations-workflow`. **55/136 tasks done.** Phase 1 + Phase 2 Foundation (minus T020) + US1 (minus T046/T047 Web) + US2 backend (T049–T058) complete. Next: US3–US6 backend, or resolve deferred Web/T020.

## Deferred items (6)

1. **T020** — Drizzle snapshot drift (TTY `drizzle-kit introspect`).
2. **T046/T047** — US1 M3U/EPG sync → preview UI integration (dirty worktree).
3. **T059/T060/T061** — US2 channel list/detail/trash UI (dirty worktree).
All Web tasks: components/hooks are ready; integration happens during UI migration merge.

## Work completed this session (T049–T058)

### US2 tests (T049–T053)
- **T049 (LIVE, 18 tests pass)**: lifecycle state machine (canTransition), lifecycleState derivation, purge preconditions (FR-016), sourcePresence orthogonality (FR-014).
- T050–T053: application/HTTP/output/Web lifecycle tests (`describe.skip`, un-skip when implementations land).

### US2 backend implementation (T054–T058)
- **T054**: `CanonicalChannelModel.canTransitionTo()` + existing `lifecycleState()`/`canPurge()`/`shouldBeInOutput()`.
- **T055**: 4 use cases — `change-channel-lifecycle` (If-Match transition), `prepare-batch-lifecycle` (stable IDs/names preview), `restore-trashed-channel`, `purge-channel` (preview + apply, refuses if not eligible).
- **T056**: canonical repository — `updateIfVersion` (optimistic), `findTrashed`, `countByLifecycle`, findAll lifecycle filter.
- **T057**: lifecycle operations routed via the operation controller (T043) as `channel_lifecycle_batch`/`channel_purge` kinds — avoids touching the dirty `output.controller.ts`.
- **T058**: `generate-m3u-output` + `generate-xmltv-output` now filter by `lifecycle: "active"` + `shouldBeInOutput()` (FR-012).

## Validation state (all green)

types/backend-core/api/worker/web: tsc **0**, lint **0**. Tests: backend-core 15, api **156** (+38 skip), worker 6, web 9. Worker architecture boundary guard still passes.

## Recommended next actions

1. **Resolve T020** in a TTY terminal.
2. **Begin US3 (T062–T074)** — EPG matching workbench. Builds on the EPG use cases (T039) + four-class classification already in place.
3. Wire Web tasks (T046/T047/T059–T061) during UI migration merge.

## Suggested skills

- `speckit-implement` — continue to US3.
- `ant-design` — Web tasks.
- `drizzle` — T020.
