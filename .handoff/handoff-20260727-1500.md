# Handoff — US1 tests + T035 done; T036–T048 implementation remaining

## Current objective

Continue `004-safe-operations-workflow`. **Phase 1 + Phase 2 Foundation (minus T020) + US1 tests (T028–T034) + T035 repository implementations are complete.** **34/136 tasks done.** Next: US1 implementation T036–T048 (use cases, worker jobs, HTTP, web, rollout runbook).

## Source of truth (read before continuing)

- `specs/004-safe-operations-workflow/{spec,plan,tasks,data-model,research,quickstart}.md` + `contracts/*`
- `specs/004-safe-operations-workflow/antd-research.md` (T001 — required before UI tasks T044–T047)
- `.specify/memory/constitution.md`
- Previous handoffs: `handoff-20260727-1330.md` (Phase 2a/2b/2c) + `handoff-20260727-1430.md` (Phase 2d/2e/2f)

## Work completed this session (T028–T035)

### US1 red tests (T028–T034) ✅
- **T028**: `safe-source-sync.repositories.test.ts` — stable upsert / mark-missing / reappear / source-scoped lookup. `describe.skipIf(!dbReachable)` — skips until T020 migration runs.
- **T029**: `m3u-sync-operation.test.ts` — preview side-effect-free, manual-field preservation, stale-fingerprint rejection, replay no-op. `describe.skip` (use cases land in T036).
- **T030**: `epg-match-operation.test.ts` — manual lock preserved, canonical ID stable, manual streams/health preserved, 4-class classification. `describe.skip`.
- **T031**: `recovery-operation.test.ts` — creation-failure zero-write, atomic-apply rollback, restore round-trip, idempotent restore. `describe.skip`.
- **T032**: `architecture-boundaries.test.ts` (**LIVE, passes**) — scans Worker `application/` + `domain/` source files for forbidden Drizzle/BullMQ/fs imports. `safe-operation-jobs.test.ts` (**LIVE, passes**) — JobRunner lifecycle (running→succeeded, failed rethrow, unknown-kind rejection). `cleanup-operation-state.test.ts` `describe.skip` (T041).
- **T033**: `operation.controller.test.ts` — `/operations/previews` 202, summary counts, Idempotency-Key requirement, preview-stale 409, cancel-on-applying 409, problem+json. `describe.skip`.
- **T034**: `operation-preview.test.tsx` — Part A (**LIVE**, validates T005 fixture builders); Part B `describe.skip` (T045 component render expectations).
- **All tests compile clean** (api tsc 0, lint 0). Skipped tests go green as their implementation tasks land.

### T035 — stable-upsert repositories ✅
- `channel.repository.ts`: implemented `upsertStable` (onConflictDoUpdate on channelIdentity — updates source facts + restores sourcePresence, never touches id/operator/health), `findBySourceAndIdentity`, `markMissing` (set sourcePresence=missing without deleting).
- `canonical-channel-member.repository.ts` (new): upsert/deactivate/findByCanonical/findBySource/findActiveBySource.
- `source-channel-identity-alias.repository.ts` (new): resolveAlias/countActiveTargets (ambiguous→blocker)/create/findBySourceChannel.

## Validation state (all green)

- types: tsc 0. backend-core: tsc 0, 15 tests. api: tsc 0, lint 0, **138 tests** (+ 22 skipped). worker: tsc 0, **6 tests** (+ 5 skipped). web: tsc 0, 7 tests (+ 3 skipped).

## Remaining US1 implementation (T036–T048)

| Task | File(s) | What |
|------|---------|------|
| T036 | `application/operation-safety/{prepare,find,update-decisions,cancel,apply}-operation.use-case.ts` | API preview/apply orchestration; consumes T024 repos + backend-core diff-engine |
| T037 | `worker/application/operation-safety/{prepare,apply}-m3u-sync.use-case.ts` | Worker M3U staging snapshot + stable diff + atomic upsert (via ports only) |
| T038 | `worker/application/operation-safety/reconcile-canonical-channels.use-case.ts` | Incremental canonical reconciliation preserving override/lifecycle/streams/health |
| T039 | `worker/application/operation-safety/{prepare,apply}-epg-match.use-case.ts` | EPG candidate staging + 4-class summary + approved-binding-only apply |
| T040 | `application/operation-safety/{create-recovery-point,prepare-recovery-restore}.use-case.ts` + `worker/.../apply-recovery-restore.use-case.ts` | Recovery point create/verify + compensating restore |
| T041 | `worker/application/operation-safety/cleanup-operation-state.use-case.ts` + `infrastructure/{database/operation-execution.repository,queue/operation-worker,queue/operation-cleanup-worker}.ts` + `worker-bootstrap.ts` | Worker op repos, dedup, 2-min lease + 30s heartbeat, stage logs, reference-safe cleanup |
| T042 | `application/task-execution/enqueue-sync.use-case.ts` + `infrastructure/bullmq/{task-queue.adapter,bullmq.module}.ts` | Pass requestId/changeSetId/fingerprint/scope/dedupId/TaskRef through enqueue |
| T043 | `http/operation/{operation.controller,operation.module}.ts` + wire into `http.module`/`infrastructure.module` | Operation HTTP endpoints + register repos/use cases |
| T044 | `web/features/dashboard/operations/operation-queries.ts` | change-set/task query keys, preview/apply mutations, target-scoped pending hooks |
| T045 | `web/features/dashboard/operations/{operation-preview,operation-impact-table}.tsx` | Reusable impact summary, warnings/blockers, paged item table, controlled confirmation (T034-B goes green) |
| T046 | `web/features/dashboard/sources/source-list-page.tsx` | M3U sync entry → preview→confirm→task, source-level loading |
| T047 | `web/routes/dashboard/epg-matching.tsx` | EPG auto-match entry → preview→confirm→task |
| T048 | `specs/004-safe-operations-workflow/rollout-runbook.md` + `apps/api/src/shared/config/safe-operations.config.ts` + `apps/worker/src/infrastructure/config/safe-operations.config.ts` + `docker/.env.example` | expand/backfill/shadow/enable/contract switch matrix + gates + rollback steps; shadow compare + per-source enable flags (after runbook review) |

**Suggested order**: T036 (API orchestration) → T037/T038 (Worker M3U) → T040 (recovery) → T041 (Worker jobs + cleanup) → T042 (enqueue trace) → T043 (HTTP) → T044/T045 (Web queries + preview component) → T046/T047 (route integration) → T048 (rollout runbook, required before any shadow/enable).

## T020 still deferred

Pre-existing Drizzle snapshot drift (0000_snapshot records early schema). Resolution: `drizzle-kit introspect` against live `magi-postgres` in a TTY terminal, then `db:generate` for the additive migration. T028 integration tests go green after this.

## Important workspace state

- Dirty UI worktree preserved (untouched).
- `apps/api/drizzle/meta/_journal.json` at 0007 (clean). `drizzle.config.ts` lists schema files explicitly.
- drizzle-orm 0.45.2 across all packages.
- Existing infra repositories still carry stubs for non-US1 methods (canonical `updateIfVersion`/`findTrashed`→T056, stream `reorder`→T115, task `findByScope`/`findSummary`→T024/T082). These are filled by their US tasks.
- No commit created.

## Suggested skills

- `speckit-implement` — continue US1 implementation.
- `nestjs-expert` — T036/T043 use-case + controller wiring.
- `testing` — un-skip T029–T031/T033 tests as use cases land.
- `ant-design` — required before T045–T047 UI work (read T001 antd-research.md first).
