# Handoff — T036 API orchestration done; T037–T048 Worker/HTTP/Web/rollout remaining

## Current objective

Continue `004-safe-operations-workflow` US1 MVP. **35/136 tasks done.** T036 (API preview/apply orchestration) complete. Next: Worker use cases (T037–T041), enqueue trace (T042), HTTP controller (T043), Web (T044–T047), rollout runbook (T048).

## Source of truth (read before continuing)

- `specs/004-safe-operations-workflow/{spec,plan,tasks,data-model,research,quickstart}.md` + `contracts/*`
- `specs/004-safe-operations-workflow/antd-research.md` (T001 — required before T044–T047)
- Previous handoffs: `handoff-20260727-1430.md` (Foundation) + `handoff-20260727-1500.md` (US1 tests + T035)

## Work completed this session (T036)

### T036 — API preview/apply orchestration use cases ✅
Five use cases in `apps/api/src/application/operation-safety/`:
- **`prepare-operation-preview.use-case.ts`**: creates `preparing` change-set + pending task, enqueues Worker `operation-prepare` job with full trace (requestId/changeSetId/fingerprint/scope/dedupId). Side-effect free on current output.
- **`find-operation-change-set.use-case.ts`**: reads change-set + paginated items (delegates to `OperationChangeSetRepository.findItems`).
- **`update-change-decisions.use-case.ts`**: records operator decisions; only `ready` accepts; conflict items need valid decision; If-Match on version.
- **`cancel-operation-preview.use-case.ts`**: cancels preparing/ready; requests task cancellation; If-Match.
- **`apply-operation.use-case.ts`**: the safe-apply protocol — idempotency check → verify ready/not-expired → verify version → acquire scope lease (2-min TTL) → create recovery point (failure ⇒ 503 zero-write, FR-018) → transition to applying → enqueue Worker `operation-apply` job.

Also fixed: `Task` interface gained `changeSetId?`/`recoveryPointId?` (were missing in T022). `TaskQueuePort.enqueue` used via existing 3-arg signature (T042 will add the queue-name overload).

## Validation state (all green)

types/backend-core/api/worker/web: tsc **0 errors**. api: lint 0, **138 tests** (+22 skipped). No regressions.

## Remaining US1 work (T037–T048)

### Worker (T037–T041) — depends on T027 boundary + T036 dispatch
- **T037**: `worker/application/operation-safety/{prepare,apply}-m3u-sync.use-case.ts` — M3U staging snapshot (SourceImportSnapshotRepository), stable diff (backend-core computeChangeItems), atomic upsert apply (via ports only).
- **T038**: `reconcile-canonical-channels.use-case.ts` — incremental canonical reconciliation preserving override/lifecycle/manual streams/primary/health.
- **T039**: `{prepare,apply}-epg-match.use-case.ts` — EPG candidate staging, 4-class summary, approved-binding-only apply.
- **T040**: `create-recovery-point.use-case.ts` (API) + `prepare-recovery-restore.use-case.ts` (API) + `apply-recovery-restore.use-case.ts` (Worker) — per-object snapshot, compensating restore.
- **T041**: `cleanup-operation-state.use-case.ts` + `infrastructure/database/operation-execution.repository.ts` + `infrastructure/queue/{operation-worker,operation-cleanup-worker}.ts` + modify `worker-bootstrap.ts` — register prepare/apply/restore/cleanup jobs, dedup, lease+heartbeat, reference-safe cleanup.

### API (T042–T043)
- **T042**: extend `enqueue-sync.use-case.ts` + `bullmq/task-queue.adapter.ts` + `bullmq.module.ts` — pass requestId/changeSetId/fingerprint/scope/dedupId/TaskRef; add queue-name to enqueue.
- **T043**: `http/operation/{operation.controller,operation.module}.ts` + wire into `http.module`/`infrastructure.module` — POST /operations/previews, GET change-sets, PATCH items, POST apply/cancel. Un-skips T033.

### Web (T044–T047) — read T001 antd-research.md first
- **T044**: `operation-queries.ts` — change-set/task query keys, preview/apply mutations, target-scoped pending hooks.
- **T045**: `operation-preview.tsx` + `operation-impact-table.tsx` — reusable impact summary/warnings/blockers/paged table/controlled confirmation. Un-skips T034-B.
- **T046**: modify `source-list-page.tsx` — M3U sync → preview→confirm→task.
- **T047**: modify `epg-matching.tsx` — EPG match → preview→confirm→task.

### Rollout (T048)
- `rollout-runbook.md` (switch matrix/gates/stop-conditions/rollback) + `safe-operations.config.ts` (API+Worker) + `docker/.env.example`. **Must be reviewed before any shadow/enable.**

## Key wiring notes for continuers

- API use cases (T036) are constructed manually (project pattern: repos imported directly, not DI). T043 registers them in the Operation module.
- `OperationChangeSetRepository` (T024) owns both the change-set port impl AND item queries (`findItems`/`updateItemSelection`).
- Worker `JobRunner` (T027) dispatches by `JobKind`; T041 registers `operation-prepare`/`operation-apply`/`operation-restore`/`operation-cleanup` handlers via `worker-bootstrap.ts`.
- `IOperationExecutionRepository` (T027 port) is implemented in T041's `infrastructure/database/operation-execution.repository.ts`.

## T020 still deferred

Pre-existing Drizzle snapshot drift. Resolve via `drizzle-kit introspect` in a TTY terminal. Unblocks T028 integration tests.

## Validation commands

```bash
pnpm --filter @magi/api exec tsc --noEmit && pnpm --filter @magi/api lint && pnpm --filter @magi/api test
pnpm --filter @magi/worker exec tsc --noEmit && pnpm --filter @magi/worker test
pnpm --filter @magi/web exec tsc --noEmit && pnpm --filter @magi/web test
```

## Suggested skills

- `speckit-implement` — continue T037+.
- `nestjs-expert` — T042/T043.
- `ant-design` — T044–T047 (read antd-research.md first).
- `testing` — un-skip T029–T033 as use cases land.
