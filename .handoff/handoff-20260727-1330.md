# Handoff — Phase 2 Foundation partial (T006–T021 done, T020 blocked, T022–T027 pending)

## Current objective

Continue `004-safe-operations-workflow` implementation. Phase 1 (T001–T005) and the bulk of Phase 2 Foundation (T006–T019, T021) are complete. **T020 is blocked by a pre-existing Drizzle snapshot/DB drift** (not introduced by this work). T022–T027 remain.

## Source of truth (read before continuing)

- `specs/004-safe-operations-workflow/{spec,plan,tasks,data-model,research,quickstart}.md`
- `specs/004-safe-operations-workflow/contracts/*`
- `specs/004-safe-operations-workflow/antd-research.md` (T001 output — required before any UI task)
- `.specify/memory/constitution.md`

## Work completed this session (T006–T021)

### Phase 2a — Shared types (T006–T010) ✅
- New enums (const-array + literal-union, Zod-friendly): `enum/operation.ts`, `enum/channel-lifecycle.ts`, `enum/failover.ts`.
- New DTOs (strict Zod, `z.infer` only): `dto/{operation,channel-operations,task-operations,schedule,audit,backup,dashboard-operations,problem-details,concurrency}.ts`.
- Legacy `TaskStatus`/`EpgSourceType`/`ChannelStatus` moved to `enum/legacy.ts` (backward compat). New wire status is `TASK_WIRE_STATUS = "succeeded"`.
- **Naming note**: `OperationChannelVo`/`OperationStreamVo` (not `ChannelVo`/`StreamVo`) to avoid colliding with legacy handwritten VOs in `vo/index.ts` during expand phase. Aliased back when US2/US3 retire the legacy VOs.
- `dto/index.ts` and `enum/index.ts` barrels updated; types build + lint clean.

### Phase 2b — Pure diff algorithms (T011–T014) ✅
- `packages/backend-core/src/operation-diff/`: `fingerprint.ts` (SHA-256, stable sort, key-order independent), `diff-engine.ts` (add/update/mark_missing/preserve/conflict classification + manual-field protection), `types.ts`, `index.ts`.
- **15 tests, RED→GREEN**: `__tests__/fingerprint.test.ts` (8) + `__tests__/diff-engine.test.ts` (7). All passing.
- Added `vitest` devDep + `test` script + `vitest.config.ts` to backend-core. tsconfig excludes `__tests__` from dist.
- backend-core `src/index.ts` now `export * from "./operation-diff"`.

### Phase 2c — Schema (T015–T019 done; T020 BLOCKED) ⚠️
- **15 new tables** in `packages/backend-core/src/database/schema/` (single source): source-import-snapshots/items, operation-change-sets/items, operation-leases, recovery-points/items, audit-events, outbox-events, idempotency-records, canonical-channel-members, source-channel-identity-aliases, scheduled-job-configs, config-backups, channel-failover-policies.
- **7 existing tables extended** (nullable/defaulted columns only — expand phase): canonical_channels (lifecycle/version/stableKey/trashedAt/purgeAfter), channel_overrides (manualEpgLocked/manualEpgSourceId/decisionReason/version), channels (sourcePresence/firstSeenAt/lastSeenAt/missingSince/sourceRevision/version), channel_streams (origin/position/eligibleForFailover/version), m3u_sources + xmltv_sources (freshnessThresholdMinutes/lastContentFingerprint/version).
- **Schema single-source refactor (T017)**: deleted 11 duplicated schema files from `apps/api/src/infrastructure/database/schema/`. API now has **zero field definitions** — `schema/index.ts` only re-exports `@magi/backend-core` + local `auth.ts` (better-auth).
- **drizzle-orm aligned to 0.45.2 across api + backend-core + worker** (was 0.44.7 in backend-core/worker, causing cross-package type mismatch). Single version now.
- **T019 migration-gate tests** written: `apps/api/src/infrastructure/database/__tests__/safe-operations-migration.test.ts`. Skip when DB unreachable; assert lifecycle/version columns, new tables, duplicate-identity index, idempotency.
- **T020 BLOCKED**: `db:generate` produces a destructive migration (`DROP TABLE epg_sources/tasks`, `DROP COLUMN channels.name/icon/url/status` etc.) because `apps/api/drizzle/meta/0000_snapshot.json` records a very early schema that drifted from the live DB long before this feature. The live DB (`docker exec magi-postgres \d channels`) matches backend-core schema exactly. **This drift is pre-existing.** Generated migration was deleted; journal restored to `0007`. DO NOT run `db:generate` until the snapshot baseline is reconciled.

### Phase 2d — Domain (T021) ✅ (partial)
- `apps/api/src/domain/operation-safety/`: `operation-change-set.model.ts` (state machine + scopeKey + canApply), `recovery-point.model.ts` (canRestore + expiry), `operation-safety.repository.ts` (IOperationChangeSetRepository / IOperationLeaseRepository / IRecoveryPointRepository ports), `index.ts`. Pure, framework-agnostic.

## Validation state (all green)

- `@magi/types`: tsc 0 errors, lint 0, build OK.
- `@magi/backend-core`: tsc 0 errors, lint 0, **15 tests pass**, build OK, dist clean (no `__tests__`).
- `@magi/api`: tsc 0 errors, lint 0, **138 tests pass** (no regressions from schema refactor).
- `@magi/worker`: tsc 0 errors, lint 0, test (0 tests, passWithNoTests).
- Task count: 136 total, **20 marked `[X]`** (T001–T019, T021). T020 deferred (unchecked).

## T020 resolution options (decision deferred to user)

1. **`drizzle-kit pull`/introspect** to rebuild the snapshot baseline from the live `magi-postgres` DB, retiring the drifted `0000_snapshot.json`. Cleanest; rewrites `meta/_journal.json` baseline but preserves applied 0000–0007 SQL history. Then `db:generate` yields a pure additive migration (new columns + 15 new tables, no DROPs).
2. Keep T020 deferred; continue T022–T027 (pure code, no migration dependency) and resolve T020 in a TTY-capable terminal.
3. Hand-write an additive-only `0008` migration (violates the "no hand-written journal" spirit; subsequent `generate` still conflicts).

**Recommended**: option 1 in a real terminal (the interactive `db:generate`/`pull` needs a TTY; `expect` works for generate but pull is safer manual). Option 2 is acceptable to keep momentum.

## Important workspace state

- Dirty UI migration worktree (apps/web, packages/ui deletions) is **preserved** — untouched by this session.
- `specs/` still untracked as part of the broader tree.
- `apps/api/drizzle/meta/_journal.json` restored to 0007 (clean). No polluted migration files remain.
- `apps/api/src/infrastructure/database/drizzle.config.ts` now lists schema files explicitly (auth.ts + backend-core schema table files, excluding index.ts) to avoid `__exportStar` double-registration. Uses `path.resolve(__dirname, "../../../../..")` for the repo root.
- `pnpm-lock.yaml` updated (drizzle-orm alignment + vitest additions).
- No commit created.

## Recommended next actions

1. **Resolve T020** (pick option 1 or 2 above with the user).
2. **Continue Phase 2d: T022–T023** — extend channel/source/task domain models + repository ports (T022); audit/backup/schedule domain models + the shared `BackupObjectStorage` port in `packages/backend-core/src/backup/` (T023).
3. **Phase 2e: T024–T026** — implement Drizzle repositories for the new ports + HTTP infrastructure (Problem Details filter, request-context middleware, If-Match precondition, idempotency interceptor).
4. **Phase 2f: T027** — Worker Clean Architecture boundary (domain ports for job/operation/lease, application depends only on ports, Drizzle/BullMQ/fs into infrastructure).
5. After Phase 2, US1 (T028–T048) is the MVP gate.

## Suggested skills

- `speckit-implement` — continue executing the task plan.
- `speckit-analyze` — optional re-check after T020 resolution.
- `drizzle` — for the snapshot baseline reconciliation if option 1 is chosen.
- `testing` — maintain red-green for T022+ domain/repository tests.
